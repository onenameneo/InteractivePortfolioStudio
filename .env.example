PORT=3100
HOST=127.0.0.1
# First run creates .env with a random password when this value is absent.
ADMIN_PASSWORD=replace-with-a-long-unique-password
# Use true when served over HTTPS in production.
COOKIE_SECURE=false
